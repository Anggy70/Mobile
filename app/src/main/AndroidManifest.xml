<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Call"></activity>
        <activity android:name=".Sendpesan" />
        <activity android:name=".Roomdata" />
        <activity android:name=".Sharedpreferences" />
        <activity android:name=".TimeService" />
        <activity android:name=".FragmentSatu" />
        <activity android:name=".FragmentKedua" />
        <activity android:name=".MainFragment" />
        <activity android:name=".Intent" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>